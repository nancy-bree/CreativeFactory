@{
    Layout = null;
}

To: @ViewBag.To
From: admin@creativefactory.cf
Subject: @Resources.PasswordResetMsg

@Resources.Accost@ViewBag.UserName.

@Resources.ResetMsg http://localhost:4077/@Html.Raw(Url.Action("ResetPasswordConfirmation", "Account", new { id = ViewBag.ConfirmationToken }))